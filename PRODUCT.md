# China Stroll Product

## Users / Problem

China Stroll 面向来北京旅行的海外游客，优先服务家庭行程组织者、多代家庭和希望深入了解中国文化的英语用户。

用户需要在一个手机优先的产品中解决四件事：找到可信景点、在地图上理解当前位置与行程、处理来华旅行工具问题、管理个人行程与预约。现有海外地图、内容平台和本地生活服务之间存在信息割裂、语言障碍、坐标差异和动态信息可信度不足。

## Product Positioning

China Stroll 是“可信景点内容 + 共同日程 + 现场地图 + 来华工具”的北京旅行导览 PWA。AI 用于解释、推荐和提出结构化修改，但不能绕过来源、权限、版本或用户确认直接修改行程。

## Confirmed Information Architecture

移动端一级导航固定为四个模块，后续产品设计与代码不得重新引入独立的一级“行程”模块。

1. 景点
   - 最近景点与当前景点
   - 景点列表、搜索和筛选
   - 根据行程或偏好的 AI 推荐与问答
   - 景点详情、收藏、加入行程、简介、游览建议和 AI 导览
   - 景点拍照与私人旅行记录
   - 后续扩展公开推荐、本地人经验和游客经验
2. 地图
   - mapcn / MapLibre 地图视觉层
   - 行程中的景点、饭店和酒店高亮
   - 当前位置和一、三、五公里附近地点
   - 地图与地点列表使用同一个 `placeId`
   - 点击标记后提供导航与取消操作
   - 跳转 Apple Maps、Google Maps 和中国本地地图
   - 用户通过开关选择是否向关联用户共享位置
3. 工具
   - 导航与打车
   - 支付说明与汇率
   - AI 文字、语音翻译和对话
   - 景点、饭店、酒店和紧急服务热线
4. 我的
   - 个人信息
   - 行程表，覆盖时间、地点、吃和住
   - 预约表，覆盖住宿、餐厅、景点门票、交通和活动
   - 预约模板、自定义字段与 AI 草稿代填

“行程”是跨模块共享的核心对象：景点负责发现并加入，地图负责空间展示，我的负责完整编辑和管理，工具读取当前行程提供上下文服务。

## MVP Scope (In)

- 英语优先的移动端 PWA。
- 四模块底部导航和跨模块地点选择状态。
- 首批 20 个经过坐标、内容和动态游览信息审核的北京景点。
- 最近/当前景点、列表筛选、详情、收藏、加入指定日期。
- 可信文字导览、地点问答、根据行程和偏好的推荐。
- 多日行程，支持增加、修改、移动和删除地点。
- mapcn 地图、当前位置、附近筛选、行程地点高亮和第三方导航。
- 个人资料、手动预约、预约状态和 AI 代填草稿。
- 支付、汇率、常用语、翻译和紧急信息的基础工具页。
- 关联用户邀请和权限；位置共享由用户主动开启，只向已关联用户共享，并可随时关闭。
- 当天行程与重点内容的基础离线读取。

## Staged After MVP

- 公开景点社区、关注关系和推荐流。
- 好友可见与公开旅行记录。
- 自动订票、自动支付和自动调用打车服务。
- 复杂多人同时编辑。
- 完整离线地图包。
- 北京以外城市和完整多语言界面。
- 原生 iOS、Android 和微信小程序。

私人拍照记录可以在 MVP 内实现；好友可见和公开发布必须等内容审核、举报、屏蔽和隐私规则完成后开放。

## Media Policy

- 景点展示图只使用 `data/processed/place-display-images` 中的非真实照片风格图片。
- `data/50景点图片附件` 中的真实照片不进入网页公共目录、数据库媒体表或正式构建。
- 展示图通过确定性脚本复制或转换到 `apps/web/public/places`，每个可发布地点必须有且只有一个稳定映射。
- 后续如需改用真实照片，必须先取得来源、授权范围和署名方式，并通过单独的产品范围确认。

## Location Sharing Policy

- 位置共享默认关闭，必须由用户主动打开开关。
- 关联用户定义为已经接受同一旅行邀请的成员；非成员永远不能读取位置。
- 共享以旅行为边界，开启时清楚显示共享对象、状态和最后更新时间。
- 用户可以随时关闭；关闭后立即停止上传并撤销其他成员读取最新位置的能力。
- 第一版只承诺前台、限时位置共享，不承诺后台持续跟踪。
- 不使用位置共享作为安全保证；产品同时提供集合点和手动报平安退路。

## Product Principles

1. 旅行中优先：关键信息在手机上少步骤可达。
2. 一个地点身份：景点、地图、行程、预约、记录和 AI 都引用同一个产品地点标识。
3. 有来源才能讲解：动态信息显示检查时间，过期或缺少依据时明确提示复查。
4. AI 修改必须确认：AI 只生成草稿和建议，用户确认后服务端再检查权限与版本。
5. 默认保护隐私：位置、订单号、偏好和私人记录按最小可见范围处理。
6. 基础能力不依赖 AI：地图入口、紧急信息、已保存行程和已审核导览在模型不可用时仍可使用。
7. 图片策略一致：正式界面不混用真实照片与展示插画。

## Success Criteria

- 至少 80% 的目标测试用户能独立完成：找到景点、查看详情、加入指定日期、在地图中定位、打开第三方导航。
- 用户能理解 AI 推荐的理由，并知道修改需要自己确认。
- 首批 20 个地点核心事实错误为零，动态信息可追溯到来源。
- 未经确认的 AI 行程写入为零。
- 无权限成员读取旅行、预约或共享位置成功为零。
- 位置共享默认关闭，关闭后的客户端不再上传位置。
- 390px 宽度下四模块主要流程无横向溢出，并具备加载、空、错误和成功反馈。
- 类型检查、lint、自动测试、生产构建和本地数据库验证全部通过。

## Confirmed Decisions

- 产品一级信息架构使用“景点、地图、工具、我的”四模块。
- 景点展示只使用 `place-display-images`，不使用真实照片。
- 位置共享采用主动开关，只对关联用户开放。
- 后续开发固定在当前 Mac 上执行，依赖必须在本机按锁文件安装，不能复制另一台电脑的 `node_modules`。
- 首批大型景点维持单一地点记录，内部看点作为导览段落，不拆成独立地点。

## Open Questions

- 位置共享默认有效时长，以及旅行结束后是否自动关闭。
- 关联用户是否只包括注册成员，还是后续增加带有效期的只读邀请链接。
- 正式底图和道路路线供应商。
- 汇率、打车与语音翻译的正式供应商和费用上限。
- 社区开放前的审核和举报运营方式。
